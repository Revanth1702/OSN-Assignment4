#include "kernel/types.h"
#include "kernel/stat.h"
#include "user/user.h"

void strace(uint mask)
{

   //The program should print the details of the system calls the command undergoes if the syscall number is set in the mask


}